# Configurações do Django
DEBUG=True
SECRET_KEY=django-insecure-muda-isso-em-producao
ALLOWED_HOSTS=localhost,127.0.0.1

# Banco de Dados (Padrão SQLite para desenvolvimento)
# Para produção, descomente e preencha as variáveis abaixo
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
# DB_USER=
# DB_PASSWORD=
# DB_HOST=
# DB_PORT=

# Configurações de JWT (Opcional se não usar API)
JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

# CORS (Para integrações externas)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
